# Tang Primer 25K + PMOD-AUDIO : Intel-style chime

## ファイル構成
- `top.sv` — トップモジュール（クロック・リセット・出力ピンの接続のみ）
- `por_reset.sv` — 電源投入リセット生成（同期リセット、`initial`不使用）
- `jingle_player.sv` — 音階シーケンサ＋ウェーブテーブル音源（DDS位相アキュムレータ＋倍音構成波形ROM＋アタック/ディケイ・エンベロープ）＋8bit PWM DAC。ピアノに寄せた音色にするため単純な矩形波駆動から変更（ROMはcase文で実装、synthesis-safe）
- `top.cst` — Gowin物理制約ファイル
- `top.sdc` — 50MHzクロックのタイミング制約

## 設計判断・前提条件（要確認）
1. **システムクロックピン**: 依頼文に具体的なピン番号の記載が無かったため、Tang Primer 25Kコアボードのオンボード発振器ピン **E2**（50MHz）を前提にしました。実機のスケマティックと異なる場合は `top.cst` の `IO_LOC "clk" E2;` を修正してください。
2. **音声出力**: 物理制約表で `Port1(G11)` のみ `Input_L` と明記されていたため、モノラル1本（G11）のみを駆動する設計にしています。PMOD-AUDIO v1.2のスケマティック（J1コネクタ）を確認したところ、ピン1=IL、ピン2=IRで、オンボードのRPフィルタ経由でPAM8403アンプ（ヘッドホン/スピーカー出力）に入っています。ステレオ化したい場合はPort2(D11)に同じ信号（またはIR用の別出力）を追加接続してください。
3. **駆動方式**: 以前は単純な矩形波（1ビットトグル出力）でしたが、ピアノらしい音色にするため以下のウェーブテーブル音源＋PWM DACに変更しました。
   - **波形ROM**: 倍音を8本（1〜8倍音、振幅比 1.0, 0.55, 0.30, 0.18, 0.10, 0.05, 0.03, 0.02 で減衰）合成した256サンプルの単周期波形をピアノ的な音色として使用（`wave_rom`関数、synthesis-safe case文）
   - **DDS位相アキュムレータ**: 16bit、音サンプルレート約48kHz（`AUDIO_DIV=1042`）ごとに`phase_inc`（音階ごとのROM値）を加算し、波形ROMを読み出し
   - **エンベロープ**: 発音開始で高速アタック（約0.7msでフルスケールへ）→ 指数関数的ディケイ（約0.8ms毎に`env -= env >> 6`）という、ピアノやハンマー打弦楽器に近い減衰特性を付与
   - **PWM DAC**: 波形サンプル×エンベロープの積を8bit PWMデューティに変換し、CLK/256（約195kHz）のキャリアで出力。PMOD-AUDIO側のRCフィルタでさらに平滑化されます
   - 休符ステップ（rest）ではエンベロープを強制的に0にして無音にしています
4. **メロディ**: 「インテル入ってる」CMで有名な5音チャイム（F#-C#-F#-C#-G#、通称"bong"）を実装しています。オクターブは4th octave（F#4≈370Hz, C#4≈277Hz, G#4≈415Hz）を仮定した近似値です。周波数（`phase_inc`）・長さ・波形・エンベロープは `jingle_player.sv` 内のパラメータで自由に調整できます。
5. **再生タイミング**: リセット解除後すぐに1回再生し、末尾に2秒の無音区間を挟んで自動的にループする構成です。ボタン等のトリガ入力は制約表に含まれていなかったため未実装です。

## ビルド手順（Gowin EDA / GW5A-LV25MG121C1）
1. 新規プロジェクトを作成し、デバイスを `GW5A-LV25MG121C1/I0` に設定
2. `top.sv`, `jingle_player.sv`, `por_reset.sv` をソースに追加
3. `top.cst` をConstraintsに追加
4. `top.sdc` をSDCとして追加（Place & Route前にタイミング制約として読み込み）
5. Synthesize → Place & Route → プログラム書き込み

## 音量・配線に関する注意
- PMOD-AUDIO v1.2の3.5mmジャック出力（OUTL/OUTR）または基板上スピーカーから音が出ます。ボリュームは基板上のポテンショメータで調整してください。
